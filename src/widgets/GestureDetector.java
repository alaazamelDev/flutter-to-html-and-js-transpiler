package widgets;

import properties.Property;

import java.util.List;

public class GestureDetector extends Widget {
    public GestureDetector(List<Property> properties) {
        super("GestureDetector", properties);
    }
}
