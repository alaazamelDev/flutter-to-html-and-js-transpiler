Scaffold(
    screenName: "ContactUs",
    appBar: AppBar(
        title: 'Contact Us',
        backgroundColor: #0E1116,
        centerTitle: true,
    ),
    body: Center(
        child: Padding(
            padding: EdgeInsetsSymmetric(horizontal: 60, vertical: 40),
            child: Container(
                width: 720,
                decoration: BoxDecoration(
                    color: #FFFFFF,
                    borderRadius: BorderRadiusCircular(radius: 16)
                ),
                child: Padding(
                    padding: EdgeInsetsSymmetric(horizontal: 32, vertical: 28),
                    child: Column(
                        children: [
                            Text(
                                text: "Let's build something beautiful",
                                fontSize: 28,
                                fontWeight: bold,
                                textAlign: center
                            ),
                            Container(height: 12),
                            Text(
                                text: "Tell us about your project and we'll get back within 24 hours.",
                                fontSize: 14,
                                textAlign: center
                            ),
                            Container(height: 20),
                            Form(
                                child: Column(
                                    children: [
                                        Row(children: [
                                            Expanded(
                                                child: TextField(
                                                    id: "firstName",
                                                    label: "First name",
                                                    hint: "Ada",
                                                    border: Border(borderRadius: BorderRadiusCircular(radius: 8)),
                                                    padding: EdgeInsetsSymmetric(horizontal: 12, vertical: 10)
                                                )
                                            ),
                                            Container(width: 12),
                                            Expanded(
                                                child: TextField(
                                                    id: "lastName",
                                                    label: "Last name",
                                                    hint: "Lovelace",
                                                    border: Border(borderRadius: BorderRadiusCircular(radius: 8)),
                                                    padding: EdgeInsetsSymmetric(horizontal: 12, vertical: 10)
                                                )
                                            )
                                        ]),
                                        Container(height: 12),
                                        TextField(
                                            id: "email",
                                            label: "Email",
                                            hint: "you@company.com",
                                            border: Border(borderRadius: BorderRadiusCircular(radius: 8)),
                                            padding: EdgeInsetsSymmetric(horizontal: 12, vertical: 10)
                                        ),
                                        Container(height: 12),
                                        TextField(
                                            id: "message",
                                            label: "Message",
                                            hint: "Briefly describe your idea...",
                                            padding: EdgeInsetsSymmetric(horizontal: 12, vertical: 12),
                                            border: Border(borderRadius: BorderRadiusCircular(radius: 8))
                                        ),
                                        Container(height: 18),
                                        Button(
                                            title: "Send Message",
                                            backgroundColor: #00C9A7,
                                            titleColor: #FFFFFF,
                                            onPressed: () {
                                                getX.set("firstName", itemValue("firstName"));
                                                getX.set("lastName", itemValue("lastName"));
                                                getX.set("email", itemValue("email"));
                                                getX.set("message", itemValue("message"));
                                                popUp("Thanks — we received your message!");
                                            }
                                        )
                                    ]
                                ),
                                onSubmit: {
                                    popUp("Form submitted — we'll be in touch.");
                                }
                            )
                        ]
                    )
                )
            )
        )
    )
)
